# Provenance Records

Every official OYYO model release must record upstream model identity, exact revision, source URLs/identifiers, license, conversion or fine-tuning lineage, quantization process, checksums, build environment where material, benchmark version and release date.
